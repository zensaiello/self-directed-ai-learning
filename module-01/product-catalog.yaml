# RETIRED after Module 1 — do not apply in subsequent modules
# Purpose: demonstrated naked Pod behavior and the problem Deployments solve
# Replaced by: product-catalog Deployment in module-02/product-catalog-deployment.yaml
apiVersion: v1
kind: Pod
metadata:
  name: product-catalog
  labels:
    app: product-catalog
    track: kubernetes-lab
spec:
  containers:
    - name: product-catalog
      image: hashicorp/http-echo:0.2.3
      args:
        - "-text=product-catalog service - version 1.0"
      ports:
        - containerPort: 5678
